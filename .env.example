# AWS region for deployment and local SDK calls
AWS_REGION=us-east-1

# DynamoDB table name (set automatically by CDK on the deployed Lambda)
DYNAMODB_TABLE_NAME=InboundSmsMessages

# Optional: override for local testing when not using deployed resources
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=
